# Local dev target: a Spring Boot backend run locally on its default port.
# application.yml's CORS_ALLOWED_ORIGIN default (http://localhost:5173)
# already matches this Vite dev server's port, so no backend config change
# is needed to develop against a local backend instance.
VITE_API_BASE_URL=http://localhost:8080
